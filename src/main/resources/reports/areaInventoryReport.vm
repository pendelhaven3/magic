$report.center("AREA INVENTORY REPORT - ${inventoryDate}", 78)
	
Report No.      : $report.field($areaInventoryReport.reportNumber, 30)            Page   :    $currentPage x $totalPages
Area            : $area
Checker         : $report.field($areaInventoryReport.checker)
Double Checker  : $report.field($areaInventoryReport.doubleChecker)
	
    Code              Product Description                   Unit Qty  Recount
------------ ---------------------------------------------- ---- ---- -------
#foreach($item in $items)
$report.field($item.product.code, 12) $report.field($item.product.description, 46) $report.field($item.unit, 4) $report.field($item.quantity, 4, "right") [     ]
#end
#if($isLastPage == true)
-----------------------------------------------------------------------------
#end