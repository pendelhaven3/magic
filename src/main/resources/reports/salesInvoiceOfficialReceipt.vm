


                                                               Page: $currentPage x $totalPages




                $report.field($salesInvoice.customer.name, 34)          $transactionDate


                $report.multiline($salesInvoice.customer.address, 34, 1)
                $report.multiline($salesInvoice.customer.address, 34, 2)




#foreach($item in $items)
    $report.field($item.quantity, 4, "right")  $item.unit   $report.field($item.product.description, 34)      $report.field($item.unitPrice, 9, "right")  $report.field($item.amount, 10, "right")

#end
#if($isLastPage == true)
#foreach($fillerLine in $fillerLines)


#end
                                                                 $report.field($salesInvoice.vatableSales, 12, "right")

                                                     Discount  :(  $report.field($salesInvoice.totalDiscountedAmount, 10, "right"))

                                                                 $report.field($salesInvoice.vatAmount, 12, "right")

                                                                 $report.field($salesInvoice.totalNetAmount, 12, "right")
#end